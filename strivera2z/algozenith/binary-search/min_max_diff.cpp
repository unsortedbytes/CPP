#include<bits/stdc++.h>
using namespace std;

void solve(){
    int n, k;
    cin>>n>>k;
    int arr[n];
    for(int i=0;i<n;i++){
        cin>>arr[i];
    }

    int 
}

signed main(){
    // basic things to add
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    // test case 
    int t;
    cin>>t;
    while(t--){
        solve();
    }
}